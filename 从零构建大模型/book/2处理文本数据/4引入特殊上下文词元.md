2.4 节的逻辑很清晰:**给 2.3 节的分词器打补丁**,让它不再因为遇到陌生词而崩溃。

# 2.4 引入特殊上下文词元

## 一、要解决的问题

回顾 2.3 节最后那个错误:

```python
tokenizer.encode("Hello, do you like tea?")
# ❌ KeyError: 'Hello'
```

**原因**:`Hello` 没出现在 The Verdict 里,所以没进词汇表,查不到 ID 就崩了。

**两个思路**(书里都讲了):

- **本节方案**:加一个 "我不认识" 的占位词元 `<|unk|>`,所有陌生词都映射到它
- **GPT 实际方案**:用 BPE 把陌生词拆成子词,根本不需要 `<|unk|>`(下一节讲)

本节先实现简单方案,理解清楚之后再去看 BPE 就轻松了。

---

## 二、两个新词元的作用

### 1. `<|unk|>` — Unknown,未知词

```
"Hello" → <|unk|> → 1131
```

任何词汇表里没有的词,统一替换成这个标记。**信息有损**,但至少不会崩。

### 2. `<|endoftext|>` — 文档边界标记

LLM 训练用的语料通常是把成千上万篇独立文档拼成一个超长字符串。问题是:

- 一篇讲菜谱的博客
- 紧跟着一篇讲量子力学的论文

这两篇内容毫无关联,但拼接后看起来像是连续的文本。如果不加分隔,模型可能学到错误的"上下文延续"——以为讲完红烧肉的下一句话应该是讲薛定谔方程。

**解决方法**:每两个独立文档之间插入 `<|endoftext|>`,告诉模型"这里是边界,前后不要硬关联"。

```
[菜谱文档] <|endoftext|> [论文文档] <|endoftext|> [小说文档] ...
```

---

## 三、改动:加两行代码

### 词汇表的修改

```python
all_tokens = sorted(list(set(preprocessed)))
all_tokens.extend(["<|endoftext|>", "<|unk|>"])   # ← 新增这行
vocab = {token: i for i, token in enumerate(all_tokens)}
```

词汇表大小从 **1130 → 1132**,两个新词元被附加到末尾。

### 分词器的修改:`SimpleTokenizerV2`

只在 `encode` 里多加**一行 if 判断**:

```python
preprocessed = [item if item in self.str_to_int
                else "<|unk|>" for item in preprocessed]
```

意思就是:**"如果词元在词汇表里就保留原样,否则替换为 `<|unk|>`"**。

替换之后再查表,就永远不会 KeyError 了。

---

## 四、代码全跑一遍成功了 ✓。我用的是短篇片段(词汇表才 144),所以未知词比书上多——但**机制完全一致**:

- `143 → <|unk|>` 替代所有陌生词
- `142 → <|endoftext|>` 标记文档边界
- **不再有 KeyError**

如果是书上完整小说全文(词汇表 1132),输出会是:

```
<|unk|>, do you like tea? <|endoftext|> In the sunlit terraces of the <|unk|>.
```

只有 `Hello` 和 `palace` 是 `<|unk|>`(这两个词没在小说里出现过),其余都被正常识别。

---

## 五、补充:还有哪些常见特殊词元

不同模型设计不同,书里列了三类:

| 词元    | 名称                  | 作用                                           |
| ------- | --------------------- | ---------------------------------------------- |
| `[BOS]` | Beginning of Sequence | 标记一段文本的**起点**                         |
| `[EOS]` | End of Sequence       | 标记一段文本的**终点**(类似 `<\|endoftext\|>`) |
| `[PAD]` | Padding               | 批训练时,把短文本**补齐**到统一长度            |

### `[PAD]` 为什么需要?

神经网络批训练时,一个 batch 里的样本必须**长度一致**才能堆成矩阵。但真实文本长短不一:

```
样本 1: "Hi"                   ← 2 个词元
样本 2: "Hello world how are"  ← 4 个词元
```

不补齐就没法做矩阵运算。补 `[PAD]`:

```
样本 1: "Hi [PAD] [PAD] [PAD]"
样本 2: "Hello world how are"
```

训练时配合**掩码(mask)**告诉模型"`[PAD]` 是假的,别学它"。

---

## 六、GPT 的实际做法:**全部不要**

书里有个值得划重点的段落:

> "**GPT 模型使用的分词器并不依赖这些特殊词元,而是仅使用 `<|endoftext|>` 词元**"

GPT 的极简哲学:

- **没有 `<|unk|>`**:用 BPE 把陌生词拆成子词,任何字符串都能编码 → 永远不会遇到"未知"
- **没有 `[BOS]`/`[EOS]`**:`<|endoftext|>` 一个搞定
- **没有 `[PAD]`**:用 `<|endoftext|>` 兼任填充,配合掩码就行

所以 GPT 词汇表里**只多一个**特殊词元。这就是为什么作者要把 2.5 节(BPE)单独拿出来讲——它从根本上消除了对 `<|unk|>` 的需求。

---

## 七、本节核心要点

```
SimpleTokenizerV1 (2.3 节)            SimpleTokenizerV2 (2.4 节)
        │                                       │
   遇到未知词                              遇到未知词
        │                                       │
        ↓                                       ↓
   ❌ KeyError 崩溃                    ✓ 替换为 <|unk|>,继续工作
                                                +
                                        ✓ 文档边界用 <|endoftext|>
```

**改动量**:词汇表加 2 行,encode 加 1 行 if 判断。**思路远比代码重要**——理解为什么需要这些特殊词元。

---

## 八、自测

1. 如果我们把 `<|unk|>` 和 `<|endoftext|>` 加到词汇表的**开头**而不是末尾(ID = 0 和 1),会有问题吗?
2. 为什么 `<|endoftext|>` 这种格式用尖括号和竖线,而不是直接用 `endoftext` 这个词?
3. 假设训练数据里既有英文小说,也有 Python 代码,我们应该用 `<|endoftext|>` 分隔吗?
4. `<|unk|>` 方案有什么根本缺陷?(想想"信息量")

<details>
<summary>参考答案</summary>

1. **没有问题,但会牵连模型权重**。词汇表中的位置只是分配 ID 的顺序,数学上 ID=0 和 ID=1131 没本质区别——它们都会被映射到嵌入向量,模型自己会学。但如果想加载别人预训练的权重,**ID 必须对齐**,否则查到的嵌入完全乱套。所以约定俗成把特殊词元放末尾(或固定位置)。

2. 为了**避免和真实词汇冲突**。如果直接用 `endoftext`,万一某篇文章里真的出现这个词怎么办?加上 `<|...|>` 的特殊符号组合,几乎不可能在自然文本中自然出现,**保证唯一性**。这是一种 "命名空间隔离"。

3. **应该用**,但更好的做法是用不同的分隔标记区分领域。混合训练时,模型可能需要知道"现在切换到代码风格了"。实际工程中常用 `<|im_start|>` `<|im_end|>` 等做更细粒度的标签(ChatGPT 的对话格式就是这样)。

4. **所有未知词被压缩成同一个符号 → 信息完全丢失**。`"COVID-19 outbreak"` 和 `"Quantum entanglement"` 如果两个词都没见过,会变成 `<|unk|> <|unk|>` 和 `<|unk|> <|unk|>`——两条**完全不同的语义**,在模型眼里**长得一样**。这就是 GPT 不用 `<|unk|>` 而用 BPE 的根本原因:即便没见过整个词,也能从子词里榨出语义信息。比如 `"COVID"` 会被拆成 `["CO", "VID"]`,各自的嵌入仍能贡献语义。

</details>
